services:
  - type: web
    name: ppi-wallet-api
    runtime: node
    rootDir: api-server
    buildCommand: npm install && cd ../mcp && npm install
    startCommand: node server.js
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
